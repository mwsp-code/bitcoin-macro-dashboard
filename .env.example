# Explicit local proxy URL takes priority when set.
BTC_PROXY_URL=http://127.0.0.1:7890

# Alternatively, configure only the local mixed proxy port.
# BTC_PROXY_PORT=7890

# Set to false to disable automatic checks of common local proxy ports.
BTC_PROXY_AUTO_DETECT=true
